# v0.32.6 Documentation Overhaul — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent (recommended) or compose:execute to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite all documentation to reflect current codebase state (v0.32.6), highlight implemented Gutenberg pipeline features, update version numbers across all files and languages.

**Architecture:** Update docs in waves — core files first (CHANGELOG, version bump), then descriptive docs (README, ARCHITECTURE, AGENTS, about, MCP-HELP, COMPILE), then git operations.

**Tech Stack:** Markdown documentation, git tags, 7 languages (en/ru/de/fr/es/ja/zh)

## Global Constraints

- Version target: **v0.32.6** everywhere
- All 7 languages must be updated: en, ru, de, fr, es, ja, zh
- Follow existing doc structure and style
- No placeholders — every section must have real content
- Commit atomic, tag, push at the end

---

## File Structure

### Files to Modify
| Path | Change |
|------|--------|
| `package.json` | Version bump 0.31.1 → 0.32.0 |
| `docs/CHANGELOG.md` | Add v0.32.6 section |
| `README.md` | Version + "What's new" |
| `docs/ru/README.md` | Version + "Что нового" |
| `docs/de/README.md` | Version + "Neuigkeiten" |
| `docs/fr/README.md` | Version + "Nouveautés" |
| `docs/es/README.md` | Version + "Novedades" |
| `docs/ja/README.md` | Version + "新機能" |
| `docs/zh/README.md` | Version + "新功能" |
| `docs/ARCHITECTURE.md` | Add pipeline sections, update version |
| `docs/ru/ARCHITECTURE.md` | Add pipeline sections, update version |
| `docs/AGENTS.md` | Add PlayerProfileStore, prose mode, update version |
| `docs/ru/AGENTS.md` | Same in Russian |
| `docs/de/AGENTS.md` | Same in German |
| `docs/fr/AGENTS.md` | Same in French |
| `docs/es/AGENTS.md` | Same in Spanish |
| `docs/ja/AGENTS.md` | Same in Japanese |
| `docs/zh/AGENTS.md` | Same in Chinese |
| `docs/about.md` | Add Gutenberg pipeline section |
| `docs/ru/about.md` | Same in Russian |
| `docs/de/about.md` | Same in German |
| `docs/fr/about.md` | Same in French |
| `docs/es/about.md` | Same in Spanish |
| `docs/ja/about.md` | Same in Japanese |
| `docs/zh/about.md` | Same in Chinese |
| `docs/MCP-HELP.md` | Add /gutenberg/process endpoint |
| `docs/ru/MCP-HELP.md` | Same in Russian |
| `docs/de/MCP-HELP.md` | Same in German |
| `docs/fr/MCP-HELP.md` | Same in French |
| `docs/es/MCP-HELP.md` | Same in Spanish |
| `docs/ja/MCP-HELP.md` | Same in Japanese |
| `docs/zh/MCP-HELP.md` | Same in Chinese |
| `docs/COMPILE.md` | Add Gutenberg pipeline instructions |
| `docs/ru/COMPILE.md` | Same in Russian |

---

### Task 1: CHANGELOG + Version Bump

**Covers:** Release metadata

**Files:**
- Modify: `docs/CHANGELOG.md`
- Modify: `package.json`

- [ ] **Step 1: Add v0.32.6 section to CHANGELOG.md**

Append before the v0.32.6 section:

```markdown
## [v0.32.6] — 2026-08-10 — Gutenberg Processing Pipeline

### Gutenberg Pipeline — 59 books → 4 SQLite databases

- **Import script** (`scripts/import-gutenberg-texts.ts`) — reads .txt files + catalog → `classics.db`
- **Process script** (`scripts/process-gutenberg.ts`) — Phase A (V1 rule-based) + Phase B (V2 LLM) orchestrator
- **Expand corpus** (`scripts/expand-corpus.ts`) — fetch additional books from Gutendex API, 59 → 250+
- **MCP endpoint** `/gutenberg/process` — trigger pipeline from MCP Console UI

### Analysis & Extraction

- **AnalyzePass** (`src/mcp/gutenberg/analyze-pass.ts`) — unified chunk analysis: pre-score, scene classification, sensory tags, temporal markers
- **NarrativeExtractor** (`src/mcp/gutenberg/narrative-extractor.ts`) — LLM-based narrative structure extraction (plot arcs, character arcs, thematic motifs, moral vector)
- **Shared cleanGutenbergText** (`src/mcp/gutenberg/clean.ts`) — deduplicated text cleaning (was 3 separate implementations)
- **Era/period helpers** (`src/mcp/gutenberg/helpers.ts`) — `inferEra()`, `inferLiteraryPeriod()`, `sampleExcerpts()`

### Player Profiles

- **PlayerProfileStore** (`src/lib/player-profile-store.ts`) — standalone cross-agent player style profiles (sentence length, sensory bias, register, dialogue ratio, preferred motifs, literary sophistication)
- 14 tracked style metrics per player, confidence-weighted updates

### Literary Compiler Extensions

- **DramaturgicPass prose mode** (`src/mcp/literary-compiler/dramaturgic-pass.ts`) — generate prose templates from chunk analysis (PROSE_ARCHETYPE_KEYWORDS, generateProseTemplate)
- **New DB tables** — `narrative_arcs`, `thematic_motifs`, `quality_calibration` in classics-compiled.db

### Tests

- 4 new test files: analyze-pass, clean, helpers, player-profile-store
- Full suite: 1145 pass / 1 fail (pre-existing)
```

- [ ] **Step 2: Bump version in package.json**

Change `"version": "0.31.1"` to `"version": "0.32.0"`.

- [ ] **Step 3: Verify**

Run: `grep '"version"' package.json` — should show `0.32.0`.

---

### Task 2: README.md (root + 7 languages)

**Covers:** User-facing documentation

**Files:**
- Modify: `README.md`
- Modify: `docs/ru/README.md`
- Modify: `docs/de/README.md`
- Modify: `docs/fr/README.md`
- Modify: `docs/es/README.md`
- Modify: `docs/ja/README.md`
- Modify: `docs/zh/README.md`

- [ ] **Step 1: Update root README.md**

Replace version header `# TrueNeverStory v0.32.6` → `# TrueNeverStory v0.32.6`.

Replace "What's new" section with:

```markdown
## What's New in v0.32.6

### Gutenberg Processing Pipeline
- **59 books → 4 databases** — full pipeline from downloaded .txt files to agent-consumable SQLite databases
- **Import + Process scripts** — rule-based Phase A (V1) + LLM Phase B (V2) orchestration
- **Corpus expansion** — fetch additional books from Gutendex API (59 → 250+)
- **MCP Console endpoint** — trigger pipeline from browser UI

### Analysis & Extraction
- **AnalyzePass** — unified chunk analysis with scene classification, sensory tags, temporal markers
- **NarrativeExtractor** — LLM-based plot arcs, character arcs, thematic motifs
- **Shared text cleaning** — deduplicated 3 different strip functions into one

### Player Style Profiles
- **PlayerProfileStore** — 14 tracked metrics per player (sentence length, sensory bias, register, dialogue ratio, literary sophistication, etc.)
- Cross-agent profiles shared between Stylist and LiteraryV2Generator
```

- [ ] **Step 2: Update docs/ru/README.md**

Same changes, translated to Russian. Replace version `v0.32.6` → `v0.32.6`, update "Что нового" section.

- [ ] **Step 3: Update docs/de/README.md**

Same changes, translated to German. Version `v0.32.6` → `v0.32.6`, "Neuigkeiten" section.

- [ ] **Step 4: Update docs/fr/README.md**

Same changes, translated to French. Version `v0.32.6` → `v0.32.6`, "Nouveautés" section.

- [ ] **Step 5: Update docs/es/README.md**

Same changes, translated to Spanish. Version `v0.32.6` → `v0.32.6`, "Novedades" section.

- [ ] **Step 6: Update docs/ja/README.md**

Same changes, translated to Japanese. Version `v0.32.6` → `v0.32.6`, "新機能" section.

- [ ] **Step 7: Update docs/zh/README.md**

Same changes, translated to Chinese. Version `v0.32.6` → `v0.32.6`, "新功能" section.

---

### Task 3: ARCHITECTURE.md (en + ru)

**Covers:** Architecture documentation

**Files:**
- Modify: `docs/ARCHITECTURE.md`
- Modify: `docs/ru/ARCHITECTURE.md`

- [ ] **Step 1: Update docs/ARCHITECTURE.md version header**

Change: `> Updated for v0.32.6` → `> Updated for v0.32.6`

Add after the "State-First Pipeline" section:

```markdown
### Gutenberg Processing Pipeline (v0.32.6)

Two-phase pipeline converts raw Gutenberg .txt files into agent-consumable databases:

**Phase A (V1 — Rule-based, no LLM):**
```
classics.db → GutenbergParser → gutenberg-normalized.db (styles + FTS)
         └→ 4-pass compiler → classics-compiled.db (quest templates)
              DramaturgicPass → StylisticPass → EmotionalPass → MetadataPass → Linter
```

**Phase B (V2 — LLM-enriched):**
```
classics-compiled.db → AnalyzePass → narrative_extractor → literary.db (scene_templates + style_patterns)
```

**New tables in classics-compiled.db:**
- `narrative_arcs` — plot arc archetypes and tension points per book
- `thematic_motifs` — symbolic motifs with evolution tracking
- `quality_calibration` — LLM response quality scores

**PlayerProfileStore** — standalone cross-agent player style profiles (14 metrics), stored in `data/player-profiles.db`.
```

- [ ] **Step 2: Update docs/ru/ARCHITECTURE.md**

Same changes in Russian. Change version reference from `2026-07-05` → `2026-08-10 v0.32.6`.

---

### Task 4: AGENTS.md (en + 7 languages)

**Covers:** Agent reference documentation

**Files:**
- Modify: `docs/AGENTS.md`
- Modify: `docs/ru/AGENTS.md`
- Modify: `docs/de/AGENTS.md`
- Modify: `docs/fr/AGENTS.md`
- Modify: `docs/es/AGENTS.md`
- Modify: `docs/ja/AGENTS.md`
- Modify: `docs/zh/AGENTS.md`

- [ ] **Step 1: Update docs/AGENTS.md**

Change header: `# Agents Reference (v0.32.6)` → `# Agents Reference (v0.32.6)`

Add section before "Storage Architecture":

```markdown
## Player Style Profiles (v0.32.6)

`PlayerProfileStore` (`src/lib/player-profile-store.ts`) provides cross-agent player style profiles shared between Stylist and LiteraryV2Generator.

**Metrics tracked:**
| Metric | Description |
|--------|-------------|
| `avg_sentence_len` | Average sentence length in words |
| `sensory_bias` | Sensory detail preference (0-1) |
| `register_score` | Formal/informal register (0-1) |
| `dialogue_ratio` | Proportion of dialogue in text |
| `narrative_distance` | Close vs distant narration (0-1) |
| `action_orientation` | Action vs reflection preference (0-1) |
| `emotional_expressiveness` | Emotional detail level (0-1) |
| `preferred_pace` | slow / medium / fast |
| `literary_sophistication` | Vocabulary/structure complexity (0-1) |
| `preferred_motifs` | Top preferred narrative motifs |
| `anti_patterns` | Avoided patterns |
| `sample_snippets` | Representative text samples |
| `confidence` | Profile confidence (0-1) |

**Storage:** `data/player-profiles.db` (SQLite, WAL mode)

**Usage:** Agents call `getPlayerStyleProfile(playerId)` to get the profile. Updates happen via `updateProfile(playerId, message)` after each player interaction.
```

Update "Literary Compiler v2" section to mention prose mode.

- [ ] **Step 2: Update docs/ru/AGENTS.md**

Same changes in Russian. Version → v0.32.6.

- [ ] **Step 3-7: Update docs/{de,fr,es,ja,zh}/AGENTS.md**

Same changes in each language. Version → v0.32.6.

---

### Task 5: about.md (en + 7 languages)

**Covers:** Design rationale documentation

**Files:**
- Modify: `docs/about.md`
- Modify: `docs/ru/about.md`
- Modify: `docs/de/about.md`
- Modify: `docs/fr/about.md`
- Modify: `docs/es/about.md`
- Modify: `docs/ja/about.md`
- Modify: `docs/zh/about.md`

- [ ] **Step 1: Add Gutenberg Pipeline section to docs/about.md**

Add after the Translation Pipeline section:

```markdown
---

## Gutenberg Processing Pipeline

### Why a Multi-Phase Pipeline

Raw Project Gutenberg texts are noisy — headers, footers, encoding artifacts, varying formatting. A single-pass approach produces poor style patterns. The pipeline uses two phases:

1. **Phase A (V1, rule-based):** Deterministic text cleaning, chunk splitting, 4-pass literary compilation. No LLM cost. Produces quest templates and style patterns.
2. **Phase B (V2, LLM-enriched):** AnalyzePass pre-scores chunks, NarrativeExtractor extracts plot arcs and character arcs. Produces scene templates for LiteraryV2Generator.

### Data Flow

```
Gutenberg .txt files (59+)
  ↓ import-gutenberg-texts.ts
classics.db (raw + metadata)
  ↓ process-gutenberg.ts Phase A
gutenberg-normalized.db (styles) + classics-compiled.db (templates v1)
  ↓ process-gutenberg.ts Phase B
literary.db (scene_templates + style_patterns v2)
  ↓
Stylist ← gutenberg-normalized.db
Dramaturg ← classics-compiled.db
LiteraryV2Generator ← literary.db
```

### Why Standalone Scripts

The pipeline runs offline (not per-user-request). Standalone scripts with MCP endpoints allow:
- Manual trigger from MCP Console UI
- Background processing without blocking game sessions
- Corpus expansion without restarting the server
```

- [ ] **Step 2-7: Update docs/{ru,de,fr,es,ja,zh}/about.md**

Same section in each language.

---

### Task 6: MCP-HELP.md (en + 7 languages)

**Covers:** MCP Console documentation

**Files:**
- Modify: `docs/MCP-HELP.md`
- Modify: `docs/ru/MCP-HELP.md`
- Modify: `docs/de/MCP-HELP.md`
- Modify: `docs/fr/MCP-HELP.md`
- Modify: `docs/es/MCP-HELP.md`
- Modify: `docs/ja/MCP-HELP.md`
- Modify: `docs/zh/MCP-HELP.md`

- [ ] **Step 1: Add /gutenberg/process endpoint to docs/MCP-HELP.md**

Add to the endpoints section:

```markdown
### POST /mcp/gutenberg/process

Trigger the Gutenberg processing pipeline from the MCP Console.

**Request body:**
```json
{
  "phase": "all"  // "v1" | "v2" | "all"
}
```

**Response (SSE stream):**
```json
{"phase":"parse","pct":10,"message":"Parsed 45/59 books"}
{"phase":"compile","pct":50,"message":"Running DramaturgicPass..."}
{"phase":"analyze","pct":75,"message":"Analyzing chunks..."}
{"phase":"done","pct":100,"message":"Pipeline complete"}
```

**Phases:**
- `v1` — Phase A only (rule-based, no LLM): parse → compile → done
- `v2` — Phase B only (LLM): analyze → extract → done
- `all` — Both phases sequentially
```

- [ ] **Step 2-7: Update docs/{ru,de,fr,es,ja,zh}/MCP-HELP.md**

Same endpoint documentation in each language.

---

### Task 7: COMPILE.md (en + ru)

**Covers:** Compilation/build instructions

**Files:**
- Modify: `docs/COMPILE.md`
- Modify: `docs/ru/COMPILE.md`

- [ ] **Step 1: Add Gutenberg pipeline section to docs/COMPILE.md**

Add section:

```markdown
## Gutenberg Pipeline

### Prerequisites
- Downloaded .txt files in `data/gutenberg/texts/`
- (Optional) `data/mcp/gutenberg-catalog.db` for metadata enrichment

### Import texts
```bash
bun run scripts/import-gutenberg-texts.ts
```
Creates `data/gutenberg/classics.db` from .txt files + catalog.

### Process pipeline
```bash
# Phase A only (rule-based, no LLM)
bun run scripts/process-gutenberg.ts --phase v1

# Phase B only (LLM-enriched)
bun run scripts/process-gutenberg.ts --phase v2

# Both phases
bun run scripts/process-gutenberg.ts --phase all
```

### Expand corpus
```bash
bun run scripts/expand-corpus.ts --authors "Dickens,Tolstoy" --target 3
bun run scripts/expand-corpus.ts --authors "Hemingway" --dry-run
```
```

- [ ] **Step 2: Update docs/ru/COMPILE.md**

Same section in Russian.

---

### Task 8: Git — Commit, Tag, Push

**Covers:** Release operations

**Files:** All files modified above

- [ ] **Step 1: Stage all changes**

```bash
git add -A
```

- [ ] **Step 2: Commit**

```bash
git commit -m "docs: v0.32.6 — Gutenberg pipeline documentation overhaul

- CHANGELOG: add v0.32.6 section with all 8 features
- README: update version + What's New in 7 languages
- ARCHITECTURE: add pipeline sections (en + ru)
- AGENTS: add PlayerProfileStore, prose mode (7 languages)
- about: add pipeline rationale (7 languages)
- MCP-HELP: add /gutenberg/process endpoint (7 languages)
- COMPILE: add pipeline instructions (en + ru)
- package.json: bump to 0.32.0"
```

- [ ] **Step 3: Tag**

```bash
git tag v0.32.6
```

- [ ] **Step 4: Push**

```bash
git push && git push --tags
```

- [ ] **Step 5: Verify**

```bash
git log --oneline -3
git tag -l 'v0.32*'
```
